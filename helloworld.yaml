---
- name: Install webapp
  hosts: build
  become: true

  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes

    - name: Install required packages
      apt:
        name:
          - maven
          - openjdk-11-jdk
          - git
        state: present

    - name: Create webapp directory
      file:
        path: /home/dmitry/webapp
        state: directory
        mode: '0755'

    - name: Clone repository
      git:
        repo: https://github.com/yankils/hello-world.git
        dest: /home/dmitry/webapp
        update: yes

    - name: Build webapp
      command: mvn package
      args:
        chdir: /home/dmitry/webapp

    - name: Copy .war into prod host
      copy:
        src: /home//home/dmitry/webapp/webapp/target/webapp.war
        dest: root@158.160.23.164:/var/lib/tomcat9/webapps/

- name: Prod
  hosts: prod
  become: true

  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes

    - name: Install required packages
      apt:
        name:
          - openjdk-11-jdk
          - tomcat9
        state: present



